{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow local overlay windows to invoke application commands only.",
  "local": true,
  "windows": ["overlay", "settings"],
  "permissions": []
}
