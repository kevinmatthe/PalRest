# 观察窗口指标修复

目标：所有指标按选定窗口汇总，保留已确认变化，不把缺失当作零。

- [x] 存档世界身份：增加 `save.world_id`，验证非零 GUID，作为独立参数传给 worker。留空仍从目录识别；不重写历史未知世界证据。
- [x] 完整窗口：轨迹、快照、变化分别检查截断，按半开时间范围分段加载，合并后校验总数。保留原始纳秒时间，支持取消、超时和资源上限。
- [x] 历史小结：与地图局部回放缓存分离，汇总窗口起点至游标；不使用游标后的观测。
- [x] REST 等级：累计各有效区间；后续中断不清除此前增量。主卡展示起止等级及已确认变化合计，保留分段证据和边界说明。
- [x] 六项存档指标：保留比较边界两侧有效区间的变化，只展示部分合计，不跨边界计算净变化。
- [x] 完整窗口展示性能：原始观测与证据按 100 条分页。图表按横向像素桶保留真实首尾和极值，保留原有分段；统计仍使用全部记录。
- [x] 回归验证：Go 全量测试、前端测试、生产构建、打包存档 worker 测试；真实观测回放确认保留等级增长 +6。
交付流程：分支提交并推送，由仓库 Containers GitHub Actions 运行测试、构建和发布镜像，检查该提交的流水线结果。
